#include "../include/RecursoDigital.h"

RecursoDigital::RecursoDigital(const string& id, const string& titulo, 
                             const string& autor, int anioPublicacion)
    : id(id), titulo(titulo), autor(autor), anioPublicacion(anioPublicacion) {} 